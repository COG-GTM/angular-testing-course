{
  "name": "angular-testing-course",
  "version": "0.0.0",
  "license": "MIT",
  "description": "Mock Express REST backend for the React (Vite) migration of the Angular Testing Course. The frontend lives in react-app/.",
  "scripts": {
    "server": "ts-node -P ./server/server.tsconfig.json ./server/server.ts",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run"
  },
  "private": true,
  "dependencies": {
    "body-parser": "^1.18.3",
    "cors": "^2.8.5",
    "express": "^4.16.2"
  },
  "devDependencies": {
    "@types/express": "^4.0.39",
    "@types/node": "^12.11.1",
    "cypress": "^9.1.1",
    "ts-node": "~3.2.0",
    "typescript": "~5.6.3"
  }
}
