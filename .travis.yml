
language: node_js
node_js:
  - 20
cache:
  npm: true
  directories:
    - ~/.npm
    - react-app/node_modules
install:
  - cd react-app && npm ci
script:
  - cd react-app && npm run lint
  - cd react-app && npm run test
  - cd react-app && npm run build
